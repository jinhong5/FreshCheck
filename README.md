# Fresh Check

## Backend

- **Install dependencies**  
  `cd backend && npm install`

- **Run in development**  
  `npm run dev`

- **Health check**  
  `GET http://localhost:3000/api/health` → `{"ok": true}`
